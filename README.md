# Steve’s Personal News Reader (GitHub Pages)

Static personal news site. Bots refresh `index.html` hourly from the x-taste-reader pipeline.

## Local preview
Open `index.html` in a browser, or run: `python3 -m http.server 8080` from this folder.
